server {
    listen 80;
    server_name localhost;
    root /opt/nginx/ansible;
    
    location / {
        index index.php;
    }

    location ~ .php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php-fpm.sock; # Путь до вашего PHP-FPM сокса
    }
}